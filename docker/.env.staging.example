# ADHD Focus - Staging Configuration
# Copy this file to .env.staging and configure

# ===================
# Required Settings
# ===================

# Domain for HTTPS (staging)
DOMAIN=adhdrenaline.com

# Site URL (with protocol)
SITE_URL=https://adhdrenaline.com

# PostgreSQL password (use different from production!)
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=your-staging-secret-password-change-me

# NextAuth secret (use different from production!)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-staging-auth-secret-at-least-32-characters

# ===================
# Optional Settings
# ===================

# Database name (separate from prod)
POSTGRES_DB=adhd_focus_staging

# PostgreSQL port (local access only, staging uses 5433)
POSTGRES_PORT=5433

# Web app version (staging uses :staging tag)
WEB_VERSION=staging

# ===================
# OAuth (Optional)
# ===================

# Google OAuth (for Google Sign-In) - use same as prod or separate
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
