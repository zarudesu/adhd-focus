# ADHD Focus - Self-Hosted Configuration
# Copy this file to .env and configure

# ===================
# Required Settings
# ===================

# Domain for HTTPS (e.g., adhd-focus.example.com)
DOMAIN=localhost

# Site URL (with protocol)
SITE_URL=https://adhd-focus.example.com

# PostgreSQL password
# Generate with: openssl rand -base64 24
POSTGRES_PASSWORD=your-super-secret-password-change-me

# NextAuth secret
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-at-least-32-characters

# ===================
# Optional Settings
# ===================

# Database name (default: adhd_focus)
POSTGRES_DB=adhd_focus

# PostgreSQL port (local access only, default: 5432)
POSTGRES_PORT=5432

# Web app version (default: latest)
WEB_VERSION=latest

# ===================
# OAuth (Optional)
# ===================

# Google OAuth (for Google Sign-In)
# Create OAuth app at https://console.cloud.google.com
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
